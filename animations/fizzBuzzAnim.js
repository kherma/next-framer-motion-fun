export const errorMessage = {
  initial: { opacity: 0 },
  animate: { opacity: 1 },
  exit: { opacity: 0 },
};

export const clueMessage = (key) => ({
  key: key,
  initial: { opacity: 0 },
  animate: { opacity: 1 },
  exit: { opacity: 0 },
});
