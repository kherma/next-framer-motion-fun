import { letters } from "./letters";
import { colorSchemes } from "./colorSchemes";

const data = {
  name: "",
};

const view = {
  size: "",
  planet: "",
};

const settings = {
  letters,
  colorSchemes,
};

export const config = {
  data,
  view,
  settings,
};
